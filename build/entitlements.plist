<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTD/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened Runtime 활성화 시 Python/PyInstaller 앱이 동작하려면 다음이 필요 -->

    <!-- Python ctypes / dynamic library 로드 (numpy, cv2, paddle 등 필수) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- JIT (PyTorch / paddlepaddle 일부 백엔드) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- 화면 캡처 (mss, Playwright) -->
    <!-- 실제 권한 동의는 Info.plist 의 NSScreenCaptureDescription 으로 사용자에게 prompt -->

    <!-- 네트워크 (deep-translator, Google Vision API) -->
    <key>com.apple.security.network.client</key>
    <true/>
</dict>
</plist>

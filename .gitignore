# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
env/

# Build (PyInstaller workdir 무시 — spec/hook/entitlements 파일은 보존)
/build/*
!/build/*.spec
!/build/*.py
!/build/*.plist
/dist/
*.spec.bak

# Installer (Inno Setup 산출물 무시 — .iss 스크립트는 보존)
/installer/dist/

# PaddleOCR cached models
~/.paddleocr/
.paddleocr/

# Playwright browsers (cached system-wide, but exclude any local copy)
.playwright/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Local config
config.local.json
*.log
logs/

# OCR temp files
captures/
*.tmp.png

# Test/debug screenshots and design handoff archive (extracted version is committed)
화면 캡처*.png
ocr.zip

# bkit plugin metadata (frequently changing local state)
docs/.pdca-status.json
